rules:
  artipacked:
    ignore:
      - copyright.yaml
      - format.yaml
      - lint-and-test.yaml

  unpinned-uses:
    config:
      policies:
        actions/*: ref-pin
        oven-sh/setup-bun: ref-pin
        golangci/golangci-lint-action: ref-pin

  dangerous-triggers:
    ignore:
      - label.yaml
